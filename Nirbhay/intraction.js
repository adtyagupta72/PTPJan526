let length=prompt("Enter the length of cuboid",0)
let height=prompt("Enter the height of cuboid",0)
let width=prompt("Enter the width of cuboid",0)

let res=parseInt(length)*parseInt(height)*parseInt(width)
console.log(res)
